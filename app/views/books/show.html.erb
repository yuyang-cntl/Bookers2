<div>
<%= flash[:notice] %>
</div>
<div class="container-fluid">
 <div class="row">
  <div class="col-md-3 mt-1">
   <%= render 'list', book: Book.new, user: @user %>
  </div>

  <div class="offset-md-1 col-md-8">
   <h2 class="pt-2 pb-2 custom-heading">Books detail</h2>
   <div class="d-flex text-align-start pb-1">
  </div>

  <div class="d-flex custom-heading">
   <div class="flex-colmun col-md-2 py-2">
   <%= link_to user_path(@user.id) do %>
    <%= image_tag @user.get_profile_image(100,100) %>
    <%= @user.name %>
   <% end %>
   </div>
   <div class="col-md-3 d-flex justify-content-end py-2">
   <%= link_to book_path(@book) do %>
    <p><%= @book.title %></p>
   <% end %>
   </div>
   <div class="col-md-4 py-2">
    <p><%= @book.body %></p>
   </div>
    <div class="d-flex justify-content-between col-md-3 py-2">
     <%= button_to "Edit", edit_book_path(@book), method: :get, class: "btn btn-success" %>
     <%= button_to "Destroy", book_path(@book), method: :delete, class: "btn btn-danger", "data-confirm" => "本当に消しますか？" %>
    </div>
   </div>
  </div>
 </div>
</div>
